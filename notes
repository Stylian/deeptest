
added python3-pip debian package
added tensorflow with pip
maybe add package file ? __setup.py__ --- something like this

tensorflow ? or something with UI
maybe some type of UI

error per epoch graph to include